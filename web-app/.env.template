# React App Configuration
# API URL for backend server
REACT_APP_API_URL=http://localhost:5000

# Production example:
# REACT_APP_API_URL=https://your-api-domain.com

# Note: Stream keys are NOT stored in .env files
# They are stored securely in the browser's localStorage via the UI
# Add your YouTube stream key through: Settings > Social Media Accounts > Manage Accounts
